# AWS SDK for Python
boto3>=1.34.0

# AWS CLI tools (included in Lambda runtime, but specified for local development)
botocore>=1.34.0

# Type stubs for boto3 services
boto3-stubs[ce,savingsplans,sns,sqs]>=1.34.0

# Testing dependencies
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.11.0
moto>=4.1.0
