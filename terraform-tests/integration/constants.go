package test

const (
	testPrefixCurrent     = "sp-autopilot-test"
	testPrefixOld         = "sp-test-"
	testPrefixCurrentDash = "sp-autopilot-test-"
	logSeparator          = "========================================"
	disabledCronSchedule  = "cron(0 0 1 1 ? 2099)"
	suffixScheduler       = "-scheduler"
	suffixPurchaser       = "-purchaser"
)
